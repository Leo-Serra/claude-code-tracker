# claude-code-tracker
Claude Token Tracker gives you instant visibility into your Claude Code usage without leaving VS Code. It reads session logs locally — no API key required — and shows token consumption by 5-hour block and weekly breakdown, estimated costs per model (Sonnet, Opus, Haiku), and a per-project summary.
